{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "cd code/exercises && composer install --no-dev --optimize-autoloader"
  },
  "deploy": {
    "startCommand": "cd code/exercises && php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}